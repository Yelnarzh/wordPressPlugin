{
  "name": "wordPressPlugin",
  "version": "1.0.0",
  "private": true,
  "description": "WordPress plugin repository — minimal package.json created to satisfy npm",
  "scripts": {
    "start": "http-server -c-1 -p 3000",
    "test": "echo No tests && exit 0"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
